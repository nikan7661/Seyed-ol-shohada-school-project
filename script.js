// Simple click effect for "Read More" buttons
document.querySelectorAll('.read-more').forEach(button => {
  button.addEventListener('click', () => {
    alert("This would open the full post in a real blog application!");
  });
});
